class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        my_dict = dict()
        for num in nums:
            if(num in my_dict):
                my_dict[num]=my_dict[num] + 1
            else:
                my_dict[num]=1
        for key, value in my_dict.items():
            if(value > len(nums)/2):
                return key
        return -1
